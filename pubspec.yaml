name: event_maker
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  connectivity_plus: ^6.1.1 # check internet connection

  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_screenutil: ^5.9.3
  flutter_svg: ^2.2.3
  get: ^4.6.6
  google_fonts: ^6.2.1 # use Inter font
  pinput: ^5.0.0
  skeletonizer: ^2.1.2
  flutter_hooks: ^0.21.3+1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  logger: ^2.6.2

flutter:

  uses-material-design: true

  assets:
    - assets/icons/
    - assets/images/
